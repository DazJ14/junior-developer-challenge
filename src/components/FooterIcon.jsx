/* eslint-disable react/prop-types */
const FooterIcon = ({ children }) => {
  return (
    <a href="#" className="rounded-full border-[3px] border-white p-2">
      {children}
    </a>
  );
};

export default FooterIcon;
